name: lambda-object-storage-database
runtime:
  name: python
  options:
    virtualenv: venv
description: Deploying a lambda that stores objects to S3 and maintains a database using DynamoDB.
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
